# Core app
dash~=2.17
dash-bootstrap-components~=1.6

# Image processing
Pillow~=10.4
numpy>=1.26,<3.0
opencv-python~=4.10

# OCR (Python bindings; needs Tesseract-OCR installed separately — bundled on
# Windows, `apt install tesseract-ocr` on Linux, `brew install tesseract` on Mac)
pytesseract~=0.3.13

# Reading text from attached PDFs (invoices, saved product pages). Extracts an
# embedded text layer directly, and renders + OCRs pages that are scans. Optional
# — PDFs are still saved as attachments without it, just not text-scanned.
PyMuPDF~=1.24

# Prod server (optional; your StartApp.bat supports it)
waitress~=2.1

# Compatibility for older Python versions
typing-extensions>=4.8 ; python_version < "3.11"

# QR codes for the Connect panel (optional; app works without it)
qrcode~=7.4

# Richer product-page HTML parsing for the importer (optional; JSON-LD/OG still
# work without it)
beautifulsoup4~=4.12
